import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        width: '100%',
        height: '100%'
    },
    texto: {
        color: '#ffffff', // Texto blanco para mejor contraste con el gradiente
        fontSize: 16,
        textAlign: 'center',
        padding: 20,
        fontWeight: 'bold'
    }
})